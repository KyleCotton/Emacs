;;File for all custom keybindings

;; Change F5 to compile
(global-set-key (kbd "s-0") 'compile)

;; Set RSS feed to command + 1
(global-set-key (kbd "s-1") 'elfeed)

;; Set IRC to command + 2

;;(global-set-key (kbd "s-2") (circe "ZNC/imaginarynet"))




(provide 'init-keybindings)
